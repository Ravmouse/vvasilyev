/**
 * 3. Реализовать собственный механизм блокировок Lock.
 * Аналогично методам интерфейса Lock. Необходимо реализовать всего два метода:
 * 1) lock - проверяет свободен ли лок? Если да - захватывает, иначе блокируется.
 * 2) unlock - проверяет владеет ли поток локом? Если да то - освобождает.
 * @version 1.0
 */
package ru.job4j.h4waitnotifynotifyall.t3lock;